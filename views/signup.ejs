<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Login</title>
    <link rel="stylesheet" href="/style.css" />
    <style>
      /* 추가된 부분: 회원가입 성공 메시지 스타일 */
      .signup-success {
        text-align: center;
        margin-bottom: 20px;
        color: green;
      }

      /* 수정된 부분: 폰트 크기 조정 */
      .form-group input {
        width: 100%;
        padding: 8px;
        font-size: 16px; /* 폰트 크기 조정 */
        border: 1px solid #ccc;
        border-radius: 3px;
      }
    </style>
  </head>
  <body>
    <header>
      <nav>
        <ul>
          <li><a href="/">Home</a></li>
          <li><a href="/about">About</a></li>
          <li><a href="/contact">Contact</a></li>
        </ul>
      </nav>
    </header>
    <div class="form-container">
      <h2>회원가입</h2>
      <!-- 추가된 부분: 회원가입 성공 메시지 -->
      <% if (typeof success !== 'undefined' && success) { %>
      <p class="signup-success">회원가입 되셨습니다.</p>
      <% } %>
      <!-- 회원가입 폼 -->
      <form action="/signup" method="post">
        <div class="form-group">
          <label for="name">이름:</label>
          <input
            type="text"
            id="name"
            name="username"
            placeholder="Enter your name"
            required
            autocomplete="off"
          />
        </div>
        <div class="form-group">
          <label for="password">패스워드:</label>
          <input
            type="password"
            name="password"
            id="password"
            placeholder="password"
            required
          />
        </div>
        <button type="submit" class="submit-btn">회원가입</button>
      </form>
      <!-- 이미 아이디가 있으십니까? -->
      <p>이미 아이디가 있으십니까? <a href="/">로그인</a></p>
      <!-- 에러 메시지 -->
      <% if (typeof error !== 'undefined' && error) { %>
      <p style="color: red"><%= error %></p>
      <% } %>
    </div>
  </body>
</html>
